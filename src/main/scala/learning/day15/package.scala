package learning

/**
 * 学习目标：
 * 1.FP风格和OOP风格在反编译后字节码上的差异
 * 2.什么叫函数的字面量
 *   (x:Int) => x +1
 * 3.什么叫目标类型？
 *   因为一个表达式的目标使用场景可以影响该表达式的类型
 *   list.filter(x => x > 0)
 * 4.占位符语法
 *   list.filter( _ > 0 )
 */
package object day15 {

}
